---
name: competitive-analysis
description: '联网搜索 GitHub 等平台查找主流竞品，按功能维度制作对比矩阵表格，输出竞品分析报告。使用场景：新项目启动时需要了解行业竞品格局、用户说"竞品分析"/"对比竞品"/"行业调研"、竞品大版本发布后需要增量更新。'
argument-hint: '说明项目领域（如 Excel/Word/PDF）、竞品搜索范围（开源/商业/二者）、是否需要拆分为多份分领域报告。'
---

# 竞品分析工作流

## 适用场景

- 新项目启动，需要了解行业竞品格局
- 用户说"竞品分析"/"对比竞品"/"行业调研"/"有哪些竞品"
- 竞品大版本发布后需要增量更新分析报告
- 为功能规划提供需求第二来源（确保不落后于任何竞品）

## 核心原则

- **以项目愿景为基准**：竞品分析服务于项目愿景目标，不做无方向的罗列
- **功能维度优先**：以功能对比矩阵为核心，按能力域分组建表，补充非功能维度（许可证/依赖/性能/兼容性/维护状态）
- **搜索以 GitHub 为主**：开源竞品优先从 GitHub 发现，商业竞品通过官网文档补充
- **大领域拆分**：如 Excel/Word/PPT/PDF 四大领域分别编写独立分析文档
- **数据可验证**：所有竞品数据必须联网搜索验证，报告首行标注版本和日期

## 执行步骤

### 1. 读取项目愿景

读取 `Doc/需求文档.md`（如存在），提取项目定位。若需求文档不存在，读取 `README.md` 头部获取项目定位。

### 2. 联网搜索竞品

**搜索策略**（按优先级）：
1. **GitHub 代码搜索**：用 `github_text_search` 搜索项目领域关键词
2. **GitHub 仓库搜索**：搜索 `topic:{领域}` 或按名称/描述搜索相关仓库
3. **NuGet 补充**：搜索 NuGet 包名，补充包大小、下载量等数据
4. **商业竞品**：通过官网文档补充

**收集信息**：每个竞品记录名称、许可证、最新版本、GitHub Stars、NuGet 下载量、支持格式、依赖大小。

### 3. 制作对比矩阵

按功能维度逐一对比，使用统一标记：

| 标记 | 含义 |
|------|------|
| ✅ | 完整支持 |
| ⚠️ | 部分可用 |
| ❌ | 不支持 |
| — | 不适用 |

**功能维度模板**（按项目类型调整）：
- **基础读写**：读/写各格式、多工作表/页面、流式处理
- **内容与样式**：字体/颜色/边框/对齐/格式
- **布局与结构**：合并/冻结/筛选/分组/分页
- **高级元素**：图表/图片/公式/超链接/批注
- **便捷 API**：对象映射/模板填充/DataTable 支持
- **打印与导出**：页面设置/页眉页脚/PDF 转换

### 4. 补充非功能维度

| 维度 | 对比项 |
|------|--------|
| 依赖与体积 | 外部依赖数、包体积、运行时内存 |
| 框架兼容性 | net45/netstandard2.0/net6.0+/net8.0+ |
| 许可证风险 | 许可证类型、商业使用限制 |
| 维护状态 | 活跃更新 / 维护模式 / 停更，最近发版日期 |
| 性能特征 | 大文件处理、流式读写能力 |

### 5. 输出竞品分析报告

**大领域项目**：输出一份总报告 + 多份分领域报告，存放在 `Doc/` 目录。

**单一领域项目**：仅输出一份 `Doc/竞品分析报告.md`。

### 6. 增量更新

竞品大版本发布时的增量更新：读取现有报告 → 仅搜索目标竞品最新变更 → 更新标记 → 补充新功能 → 更新版本号和日期。

## 输出要求

- 报告首行必须标注版本和日期
- 必须对比：功能矩阵表格（至少 3 个能力域）、非功能对比、差距分析
- 格式：Markdown 表格，UTF-8 无 BOM
- 不凭记忆编造竞品功能数据——必须联网搜索验证
